const { Node, LinkedList } = require('./linkedList')

const palindrome = head => {
  let p1, p2
  while (p2) {
    
  }


}

console.log(palindrome(new LinkedList([[]].head))) // true
console.log(palindrome(new LinkedList([[1, 1]].head))) // true
console.log(palindrome(new LinkedList([[1, 3, 3, 1]].head))) // true
console.log(palindrome(new LinkedList([[1, 3, 5, 3, 1]].head))) // true
console.log(palindrome(new LinkedList([[1, 2, 3, 4, 2, 1]].head))) // false
